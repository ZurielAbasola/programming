#include <iostream>
#include <string>

using namespace std;

class Solution {
public:
    bool isSubsequence(string s, string t) {
        int n_s = s.length(), n_t = t.length();

        for (int i = 0; i < n_s; ++i) {
            for (int j = i; )
        }
    }
};