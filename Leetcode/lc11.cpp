#include <iostream>
#include <vector>

class Solution {
public:
    int maxArea(vector<int>& height) {
        int area = 0;

        int length = height.size();
        for (int i = 0; i < height.size(); ++i) {
            for (int j = 0; j < )
        }
        
        return area;
    }
};